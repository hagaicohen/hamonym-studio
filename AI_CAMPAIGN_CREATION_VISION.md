# AI-Assisted Campaign Creation — מסמך חזון וארכיטקטורה (ADR)

> שינוי שם מ-"AI Campaign Creation": ה-AI לא יוצר קמפיין — הוא עוזר ליצור. עקבי עם החלטה 6 (AI לא עוקף אף כלל עסקי, רק ממלא Draft). שם הקובץ נשאר `AI_CAMPAIGN_CREATION_VISION.md` כדי לא לשבור קישורים קיימים.

**סטטוס:** Pipeline הליבה + שני Extractors מומשו ומאומתים בקוד (Sprints 1-4) — ר' "מצב נוכחי" למטה
**תאריך:** 2026-07-22 (עודכן לאחרונה 2026-07-24 — ר' "עדכון מרוכז" למטה ו-Pipeline Milestones עד 2026-07-24)
**שייך ל:** Campaign Studio, Entity Onboarding

## מצב נוכחי (2026-07-22)

שלוש שכבות ברורות, במכוון לא מעורבבות:

**עדכון מרוכז (2026-07-23) — לפרטים המלאים ר' זיכרון `project_ai_campaign_creation` (יומן מפורט יותר מהמסמך הזה בשלב הזה)**: מאז הכתיבה הקודמת נבנו: (א) יצירת עמותה חדשה מ-AI (טוגל מפורש: קמפיין / עמותה בלבד / עמותה+קמפיין — לא ניחוש מטקסט חופשי), כולל תיקון תור העלאת קבצים לעמותה עצמה ותיקון unique constraint שמנע יצירה מחדש אחרי מחיקה; (ב) חיפוש אינטרנט אמיתי (OpenAI `web_search`, אותו מפתח API קיים, opt-in מפורש) עם cache ל-30 יום; (ג) גלריית תמונות אמיתית מקבצים שהועלו (לא רק תמונת Hero אחת); (ד) **סבב שאלות הבהרה** — כשה-AI מזהה שהסיפור חסר פרטים תפעוליים קונקרטיים (תאריכים, מספר משתתפים, עלות ליחידה) שרק מנהל הקמפיין יודע, הוא מציע עד 5 שאלות ממוקדות; מענה עליהן משכתב את הטקסט ברמת דיוק גבוהה משמעותית. בנוי כ-2 בקשות stateless נפרדות (לא session/Agent — החלטה 2 עדיין בתוקף), לא כשיחה רב-תורית. גם: AI כ-Creative Director (Gallery Curation/Design Intent/Content Strategy — ר' [AI_CREATIVE_DIRECTOR_CONCEPT.md](AI_CREATIVE_DIRECTOR_CONCEPT.md)) ו-Generation Logging (`campaign_ai_generations` — ר' Pipeline Milestone למטה).

**עדכון מרוכז (2026-07-24)**: איכות טקסט ה-Story שופרה דרך שני כללי מבנה מפורשים בפרומפט (משפט פתיחה = סיטואציה אנושית קונקרטית, לא הצגת עמותה; סגירה/CTA = השפעה קונקרטית → קריאה לפעולה → חזרה לאדם הספציפי) — נבדק ואומת שכלל מבני מפורש עובד טוב יותר ובעקביות גבוהה יותר מדוגמת קמפיין בודדת (ממצא מתודולוגי, לא רק תיקון נקודתי — ר' Pipeline Milestone למטה). `interleaveStoryWithImages()` (שילוב תמונות בין פסקאות הסיפור, במקום בלוק גלריה נפרד בסוף) אומת חי מקצה לקצה, כולל תיקון באג תצוגה אמיתי (כותרת "על המיזם" שחזרה על עצמה). **הסבב נסגר במפורש בבקשת המשתמש** — אין עבודת AI נוספת מתוכננת; הצעד הבא הוא שימוש אמיתי ואיסוף נתונים, לא עוד שיפור prompt.

**1. Proven — המסלול המלא עובד קצה לקצה, כולל יצירת קמפיין אמיתי (`hamonym-backend/src/agents/campaign-creation/` + `hamonym-app`'s `/campaigns/create/ai`)**
שלושה Extractors (✍️ Free Text — Sprint 1, 🌐 Website — Sprint 4, 📄 Document Collection — Sprint 6, כולל Vision לתמונות, ומאז Sprint 7 גם קלט משולב — טקסט+אתר+קבצים ביחד, לא tabs נפרדים) → `ExtractedFacts` → Brief (Sprint 2) → Draft Patches (Sprint 3, נחשף ב-HTTP רק ב-Sprint 7) → **קמפיין אמיתי נוצר בפועל** תחת העמותה הקיימת של המשתמש, עם ניווט אוטומטי ל-Campaign Studio להמשך עריכה (Sprint 7). מאומת עם Playwright אמיתי כולל שאילתת DB ישירה שהשדות (title/description/target_amount/CTA) אכן נשמרו נכון. ר' "קוד קיים" למטה לפירוט.

**באג אמיתי שנמצא ע"י המשתמש בבדיקה חיה (2026-07-22), תוקן ומאומת**: "יצרתי קמפיין עם AI, אני לא רואה שום טקסט". הסיבה: `draft.builder.js` (בקאנד) ממפה רק שדות שטוחים של `CampaignDraft` (title/shortDescription/category/targetAmount/heroCtaConfig) — הוא לא נוגע כלל ב-`blocks`, כי המערך הזה קיים רק ב-`createInitialDraft()` בפרונטאנד ואין לבקאנד שום ידיעה עליו. בלוק הטקסט הראשי ("על המיזם") נשאר עם `content: ""` בברירת מחדל. תוקן ע"י `applyStoryContent()` חדש ב-`ai-campaign-creation-page.component.ts` שממפה את `organizationDescription` (עם fallback ל-`shortDescription`) ישירות ל-blocks בפרונטאנד, אחרי `campaignState.patch()` — כולל `escapeHtml()` מפורש כי הטקסט יכול להגיע מתוכן אתר חיצוני דרך קלט משולב, ואסור לסמוך עליו כ-HTML בטוח. מאומת עם שאילתת DB ישירה על קמפיין אמיתי שנוצר.

**"→ חזרה לעריכת הנתונים" (2026-07-22)**: הכפתור היחיד אחרי Brief היה "נסה שוב עם קלט אחר" — ומחק את כל מה שהמשתמש הזין (טקסט/URL/קבצים). נוסף `backToEdit()` שפותח מחדש את טופס הקלט בלי למחוק כלום, כך שאפשר לתקן פרט אחד ולנסות שוב במקום להקליד הכל מחדש.

**פער אמיתי שנמצא ע"י המשתמש (2026-07-23), תוקן ומאומת: וידאו כ-Hero**: המשתמש נתן קישור יוטיוב בטקסט וביקש שישמש כ-Hero — לא קרה כלום. הסיבה: `ExtractedFacts` אספה URLs כלליים ל-`socialLinks` בלבד, ו-`socialLinks` בכלל לא מועבר הלאה ל-Brief (נשמט בין Facts ל-Brief). נוסף שדה ייעודי `heroVideoUrl` (רק youtube.com/youtu.be/vimeo.com, מאומת גם ברג'קס בקוד ולא רק דרך ה-prompt) שעובר כ-carry-over רגיל ל-Brief וממופה ב-`draft.builder.js` ל-`heroType:'video'` + `videoUrl`. מאומת בהרצה אמיתית קצה-לקצה (extraction→brief→draft).

**עוד פער שהמשתמש דיווח עליו באותה בדיקה — תוקן (2026-07-23), ר' "Pipeline Milestone (2026-07-23)" למטה לפרטים**:
**לוגו מקובץ שהועלה** — היה: קבצים שמועלים משמשים רק כקלט ל-Vision LLM (חילוץ טקסט) ונזרקים אחר כך. תוקן: שימוש חוזר ב-`UploadService`/`/api/media/upload`, קובץ מתויג "לוגו" → `campaignLogoUrl`, תמונה אחרת ראשונה → `coverImageUrl`.

**"תיצור לי עמותה" — Sprint 8 (2026-07-23): scope שהיה מוחרג במפורש, נבנה בפועל לפי בקשה מפורשת של המשתמש**. במקום זיהוי כוונה מהטקסט החופשי (LLM), הוחלט (יחד עם המשתמש) על **טוגל מפורש** במסך הקלט ("🏢 ליצור עמותה חדשה") — יצירת עמותה אמיתית מתוך AI היא פעולה מספיק משמעותית (בעלות DB אמיתית) שלא רצינו לבסס על ניחוש כוונה.

מימוש:
- `organization-registration-state.service.ts`'s `buildPayload()`/`initialState` יוצאו (exported) ונעשה בהם שימוש חוזר ישירות מה-AI page — **אותה** מיפוי שדות בדיוק שהאשף הידני מפיק, לא שכפול. עמותה שנוצרת מה-AI היא בלתי-ניתנת-להבחנה מעמותה שנוצרה ידנית (אותם כללי `is_profile_complete`/draft-vs-pending_review, אותם אילוצי DB).
- **`entityType` ו-`organizationNumber` הם 2 מתוך 3 השדות שחסימת `canSaveDraft` מחייבת** — Brief כמעט תמיד לא ימלא `organizationNumber` (אף פעם לא מנחש), ולעיתים גם לא ימצא `entityType` ברור. נבנה בלוק "עמותה חדשה" ניתן לעריכה במסך תוצאת ה-Brief (סוג ישות/שם/מספר עמותה/תיאור) עם `guessEntityTypeCode()` שממפה טקסט חופשי מה-LLM לאחד מ-6 הקודים הקבועים שהאשף הידני מציע (association/chalatz/וכו') — כשלא בטוח, נשאר ריק. כפתור "אשר וצור" **חסום עד להשלמת שלושת השדות** — זו בדיוק ההחלטה המפורשת של המשתמש: "זה חייב להיות אינטראקטיבי... לבקש מהיוזר לספק מספר עמותה ואז לחזור ולהמשיך" — מאומת שהחסימה עובדת (Playwright: הכפתור מושבת עד למילוי, משתחרר בדיוק כשמושלם).
- **"login" לעמותה החדשה** — נתגלה תוך כדי מחקר (agent) שיש שני מנגנוני context נפרדים באפליקציה (`CurrentEntityService` הישן ו-`CurrentContextService` שמזין את ה-switcher בטופבר), ושזרימת ההרשמה הידנית הקיימת מעדכנת רק את הראשון — פער קיים שלא תוקן. נבנתה תוספת `CurrentContextService.addEntityContext()` (מקבילה ל-`removeEntityContext()` הקיים) שמוסיפה את העמותה החדשה ל-`roles()` ואז `switchContext()` — `CurrentEntityService` נגרר אוטומטית דרך ה-`effect()` הקיים שלו, בלי לשכפל לוגיקה. זה בפועל *משפר* על הפער הקיים בזרימה הידנית, לא רק משחזר אותו.
- מאומת קצה-לקצה עם Playwright + שאילתת DB אמיתית: עמותה חדשה נוצרה (`status: draft`, כצפוי כש-email/phone חסרים), שורת `user_entities` עם `role: owner` נוצרה אוטומטית (side-effect קיים ב-transaction של `createEntity`), והקמפיין נוצר עם `entity_id` שמצביע נכון לעמותה החדשה.

**2. Next MVP — נשאר**
יצירת **עמותה חדשה** (לא רק קמפיין תחת עמותה קיימת) מתוך Brief — הוחלט במפורש להישאר מחוץ לסקופ ב-Sprint 7 (ר' Pipeline Milestone למטה). כרגע ה-AI Creator עובד רק כשלמשתמש כבר יש עמותה מנוהלת.

**3. Vision — רעיונות עתידיים, לא מחייבים, לא בסקופ נוכחי**
ריבוי מקורות + Facts Merger, Campaign Advisor/Readiness Check (כבר קיימים בקוד בהקשר אחר — ר' "קוד קיים"). ר' סעיף "Vision (Non-binding)" למטה.

**סטייה מודעת מהעיקרון (2026-07-22)**: Document Collection נבנה **לפני** ולידציה עם משתמשים אמיתיים על הזרימה הבסיסית — בניגוד לעיקרון "צוואר הבקבוק" (למטה) שקבע שהרחבות מנוע ממתינות לוולידציה כזו. זו הייתה בקשה מפורשת של המשתמש, לא סטייה שקטה — מתועד כדי שהעיקרון עצמו יישאר אמין (לא "תמיד נכון בלי יוצא מן הכלל", אלא ברירת המחדל שסטייה ממנה דורשת החלטה מודעת, לא רק מומנטום).

**העיקרון**: חזון יכול להתרחב בלי שה-MVP יזוז — כל תוספת ל-Vision נבדקת מול אותה משמעת (משנה MVP? מוסיפה שכבה גנרית? יש צורך מוכח?) לפני שהיא זזה משם.

**עיקרון "צוואר הבקבוק" (נקבע 2026-07-22, אחרי שני ניסיונות נוספים להרחיב את המנוע במקום לבנות UI):** הפרויקט הוכיח את **המנוע** (כל שש היכולות ב-Proven למעלה עובדות ומאומתות) אבל עדיין לא הוכיח את **המוצר** — אף משתמש אמיתי עדיין לא עבר את הזרימה. **צוואר הבקבוק כרגע הוא היעדר ממשק משתמש, לא יכולות המנוע.** לכן: כל הרחבה למנוע (ריבוי מקורות, קישורי ספרייה, מיזוג נתונים וכו') נדחית עד **אחרי** ולידציה של הזרימה הבסיסית עם משתמשים אמיתיים — לא מפני שהרעיונות רעים, אלא מפני שבניית עוד יכולות מנוע לפני שהמוצר עצמו נבחן היא הסיכון הגדול ביותר כרגע.

## הקשר

היום קיים מסלול יצירה אחד: **Campaign Studio** — אשף בן 8 שלבים (`CampaignStudioStateService`, מודל `CampaignDraft`) שמאפשר שליטה מלאה כמעט ברמת עורך עמודים (Elementor-like). זה נכון למשתמשים מתקדמים, אבל מהווה חסם כניסה גבוה לרוב המשתמשים.

המסמך הזה מקבע את ההחלטות האדריכליות להוספת נקודות כניסה נוספות — כולל כניסה מבוססת AI — **בלי** לשכפל את המודל, התהליכים העסקיים או תהליכי האישור הקיימים.

באותה נשימה, יצירת עמותה חדשה במערכת ("הקמת עמותה") נשענת על אשף ומודל נפרדים (`OrganizationRegistrationStateService`, 6 שלבים, `status: draft → pending_review → approved`). כל התייחסות ל-"AI פותח עמותה" במסמך הזה משמעה **מילוי הטופס הפנימי של המונים** — לא אינטראקציה עם רשם העמותות או כל גורם חיצוני.

## תרשים כללי (Overview)

נקודות כניסה — כולן מזינות את אותו מודל (החלטה 1):

```
                CampaignDraft
                     ▲
                     │
        ┌────────────┼────────────┐
        │            │            │
   AI Start     Quick Wizard   Campaign Studio
```

זרימת ה-AI Pipeline (החלטות 2, 3, 5):

```
Input
  │
  ▼
Extraction
  │
  ▼
ExtractedFacts
  │
  ▼
Brief
  │
  ▼
CampaignDraft
```

פירוט מלא של כל שלב בהחלטות שלהלן.

---

## החלטה 1 — סטודיו אחד, מספר נקודות כניסה

לא נבנה כמה מנגנוני יצירת קמפיין מקבילים. יש מוצר אחד — Campaign Studio — עם מספר דלתות כניסה שכולן מזינות את אותו `CampaignDraft`:

```
        Campaign Studio (CampaignDraft)
              ▲        ▲        ▲
         AI Start   Quick    Advanced
                     Wizard   Studio
```

**למה:** כל פיצ'ר עתידי על ה-draft (Theme, Rewards, Analytics, Approval וכו') נכתב פעם אחת. שום מסלול כניסה לא "מפצל" את המודל.

---

## החלטה 2 — AI v1 הוא One-shot, לא Agent

אין Session, אין Chat רב-תורי, אין ניהול Context מתמשך. הזרימה:

```
Input → Extraction → ExtractedFacts → Brief → CampaignDraft
```

משתמש נותן קלט (אתר / מסמך / טקסט חופשי), המערכת מציגה Brief אחד לאישור, ורק אז נוצר/מתעדכן ה-Draft. פשוט, צפוי, ניתן לבדיקה.

**חזון "Agent" (שאלה→תשובה איטרטיבית, "חסר לי הלוגו, העלה בבקשה") הוא הרחבה עתידית לגיטימית — לא נדרש ב-v1.** הפייפליין (החלטה 5) בנוי כך שהרחבה כזו תיכנס כשלב נוסף (`Missing Fields`) בלי לפרק את הזרימה הקיימת.

---

## החלטה 3 — הפרדת Facts מ-Brief

שני מושגים שונים לחלוטין, לא לערבב:

| | מייצג | דוגמה |
|---|---|---|
| **ExtractedFacts** | מה הצלחנו לחלץ (עובדה אובייקטיבית) | שם עמותה, מספר עמותה, אתר, תחום פעילות, has_logo |
| **Brief** | מה החלטנו לעשות עם זה (החלטה עסקית/יצירתית) | Template, יעד גיוס מוצע, טון, CTA, Hero, צבעים |

**למה זה קריטי:** מאפשר להריץ מחדש רק את שכבת ה-Brief (למשל "תן טון יותר אופטימי") **בלי** לחזור על ה-Extraction היקר/איטי (scraping, parsing). ר' החלטה 5.

---

## החלטה 4 — ExtractedFacts הוא DTO ממוקד, לא שכבת פלטפורמה

```typescript
interface ExtractedFacts {
  organizationName?: string;
  organizationNumber?: string;
  website?: string;
  category?: string;
  hasLogo: boolean;
  heroImages?: string[];
  socialLinks?: string[];
  // ...בדיוק מה ש-Extraction v1 באמת מפיק
}
```

- **לא** נבנה עכשיו Facts Engine / Knowledge Layer / Shared Platform עבור צרכנים היפותטיים (SEO, Analytics, Template Selector וכו') — אלה לא קיימים היום.
- **כן** ניתן שם ברור ל-shape, כדי שלא ייווצר דריפט משתמע בין `extractOrganization()` / `buildBrief()` / עתידיים אחרים שכל אחד מפרש `data: any` אחרת.
- כשיגיע צרכן שני אמיתי — מרפקטרים אז, לא מנחשים עכשיו.

> **עדכון (2026-07-22):** השם המקורי שנבחר כאן היה `CampaignFacts` — התברר שזה מתנגש עם type אמיתי וקיים בקוד (`campaign-advisor.types.js`'s `CampaignFacts`, משמעות שונה לגמרי — ר' §"קוד קיים" למטה). שונה ל-`ExtractedFacts` בכל המסמך.

---

## החלטה 5 — Extraction ו-Generation הם שני שלבים נפרדים ב-Pipeline

```
Input
   │
   ▼
Extractor           ← איטי/יקר: scraping, parsing מסמכים
   │
   ▼
ExtractedFacts
   │
   ▼
Brief Builder        ← זול/מהיר: קריאת LLM בודדת על נתונים קיימים
   │
   ▼
CampaignDraft
   │
   ▼
Campaign Studio (עריכה ידנית, כמו היום)
```

**דרישה מפורשת:** Brief regeneration מקבל `ExtractedFacts` כפרמטר — לעולם לא גוזר אותו מחדש מה-Input הגולמי. כך "תן טון אחר" הוא פעולה מהירה וזולה.

**נקודת הרחבה עתידית (לא ל-v1):** `Missing Fields` בין Facts ל-Brief — v1 רק מסמן שדות חסרים בתוך ה-Brief לתיקון ידני; בעתיד יכול להפוך לשכבת שאלות השלמה (multi-turn) **בלי לשנות את שאר הפייפליין**. הכללים ל"מה נחשב מספיק" לא מומצאים מחדש — נשענים על מה שכבר קיים ב-`OrganizationRegistrationStateService` (למשל: minimum לשמירת draft כבר קיים בגלל `registration_number` UNIQUE).

---

## החלטה 6 — AI לא עוקף אף כלל עסקי קיים

AI **אינו** נוגע ב:

- Validation
- Approval workflow (`draft → pending_review → approved`)
- Draft/status semantics
- Billing
- הרשאות (Permissions)

הוא רק ממלא `ExtractedFacts`/`Brief`/`CampaignDraft` — כל השאר עובר דרך אותם services קיימים בדיוק כמו מילוי ידני. במפורש: יצירת עמותה חדשה דרך AI נכנסת בתור `status: 'draft'` בדיוק כמו טיוטה שהתחילה ידנית ולא הושלמה, ומקודמת ל-`pending_review` רק כש-`is_profile_complete` (אותו מנגנון COALESCE הקיים כבר ב-`updateEntity`) — ללא endpoint נפרד, ללא קיצור דרך של אישור admin.

---

## החלטה 7 — מידע רגיש מחוץ ל-AI לגמרי — ברמת סכימה, לא רק מדיניות

חשבון בנק, כרטיס אשראי, API Keys, סיסמאות — **לא** קיימים כשדות אפשריים לא ב-`ExtractedFacts`, לא ב-`Brief`, לא בשום prompt שנשלח ל-LLM.

זו לא רק החלטת מדיניות ("אל תבקש") — זו **type-level guarantee**: אם השדה לא קיים בסכימה, אי אפשר בטעות לכתוב אליו גם אם ה-extraction "מנחש" לא נכון מתוך מסמך. פרטי סליקה/בנקאות ממשיכים לעבור אך ורק דרך המסכים הייעודיים (Cardcom Openfields עבור עמלת פלטפורמה, שלב הבנקאות הייעודי ב-wizard הרישום).

> **אימות מול קוד קיים (2026-07-22):** אותו עיקרון בדיוק כבר מיושם היום ב-`hamonym-backend/src/agents/approval/tools/entity.tool.js` — ה-`Entity` type שם מדיר במפורש `cardcom_api_password` וכל credential תשלומי אחר מהשורה הגולמית לפני שהיא מגיעה ל-Context/Prompt. ר' גם §"קוד קיים" למטה.

---

## החלטה 8 — נקודת הכניסה של המשתמש

```
איך תרצה להתחיל?

🌐 מהאתר שלנו
📄 ממסמך
✍️ אספר בכמה מילים
⚡ Wizard מהיר
🛠 Studio מתקדם
```

בחירה מפורשת (chips), **לא** drop-zone עם auto-detect ו**לא** תיבת צ'אט יחידה.

**למה:** כל chip ממופה ישירות ל-Extractor אחד — הבחירה **היא** ה-dispatch של הפייפליין, לא רק UI. אפשר להציג רק chips שיש להם Extractor מוכן (למשל בגל ראשון: רק 🌐 ו-✍️), ולהוסיף 📄 כשמוכן, בלי לשנות תפיסת מוצר.

תיבת צ'אט יחידה נפסלה במפורש ל-v1: היא מבטיחה ויזואלית יכולת רב-תורית (multi-turn) שסותרת את החלטה 2 (one-shot). כש-`Missing Fields` יתפתח ל-multi-turn אמיתי (הרחבה עתידית), ✍️ הוא המועמד הטבעי להפוך לחוויית שיחה — לא לפני שיש session אמיתי מאחוריו.

---

## החלטה 9 — אין להניח מראש שצרכני עתיד ישתמשו ב-ExtractedFacts

כל יכולת עתידית (Campaign Advisor, Readiness Check, וכו' — ר' "קוד קיים" ו-"חזון" למטה) בוחרת את מודל הנתונים שלה לפי הבעיה שהיא פותרת בפועל, לא לפי מה ש-`ExtractedFacts` כבר מכיל.

**למה:** בדיקה קצרה של שני "צרכנים היפותטיים" חשפה שהם בכלל לא רוצים את אותה צורת נתונים — ומאז אומתה מול קוד אמיתי (ר' §"קוד קיים" למטה):

- **Campaign Advisor** ("איך לשפר את הקמפיין") צריך נתוני **שלמות/ביצועים של קמפיין קיים** (hasTitle, hasHeroImage, supportersCount...). אין לזה קשר ל-Extraction בכלל.
- **Readiness Check** ("האם הקמפיין מוכן לפרסום") עובד על **`CampaignDraft`**, לא על `ExtractedFacts` — הרחבה טבעית של רעיון `Missing Fields` (החלטה 5), רק שבודק שלמות לפרסום ולא שלמות ל-extraction.

זה מאשש את הזהירות בהחלטה 4: כשמגיע צרכן שני אמיתי (או מדומיין ברצינות), הוא בדרך כלל מלמד שהסכימה המשותפת שדמיינו מראש לא הייתה נכונה מלכתחילה. לא מתכננים "גנרי" בלי צרכן שני קונקרטי.

---

## החלטה 10 — Brief אחד משולב לעמותה חדשה + קמפיין

כשקלט משתמש מכיל גם פרטי עמותה חדשה וגם פרטי קמפיין (למשל: "עמותת X פועלת כבר 12 שנה ומגייסת כסף ל-Y..."), מוצג **Brief אחד משולב** — לא שני אישורים נפרדים ברצף.

**למה:** מבחינת המשתמש אין שתי ישויות נפרדות — הוא חושב "אני רוצה להתחיל לגייס", לא "אני רוצה להקים Entity ואז Campaign". שני אישורים נפרדים מוסיפים חיכוך בלי תועלת.

מבחינה פנימית עדיין נוצרים שני אובייקטים נפרדים (`OrganizationDraft` + `CampaignDraft`, כל אחד דרך ה-service שלו — ר' החלטה 6) — ההפרדה הזו נשארת שקופה למשתמש, לא רק שכבת UI.

---

## החלטה 11 — מיקום הקוד: `src/agents/`, לא תיקיית-על נפרדת

בבדיקת הקוד הקיים (2026-07-22) התגלה ש-`ApprovalAgent` ו-`CampaignAdvisorAgent` **כבר קיימים ופועלים** ב-`hamonym-backend/src/agents/` — לא היפותטיים כפי שהמסמך הניח קודם (ר' §"קוד קיים" למטה). יש להם shape זהה למה שתוכנן כאן: `Context (tools) → Facts (דטרמיניסטי) → Prompt → LLM → Response`, כולל `llm.service.js` ו-`trace.util.js` משותפים.

**החלטה:** קוד ה-AI Campaign Creation נכנס תחת אותה תיקייה — `src/agents/campaign-creation/` — **לא** תיקיית-על נפרדת (`src/ai/` וכו'). המילה "Agent" בקונבנציה הקיימת לא מרמזת autonomy/multi-turn — היא רק מסמנת "יכולת LLM חד-פעמית". פיצול תיקיית-העל למי שמחפש "איפה גר קוד AI" גרוע יותר מהערך הסמנטי של ההבחנה Agent/Pipeline.

ההבחנה בין "שופט/מייעץ" (Approval, Advisor) ל"בונה state חדש" (Creation) מסומנת **בשם הקובץ הראשי**, לא במיקום:

```
src/agents/campaign-creation/
  campaign-creation.pipeline.js   ← לא .agent.js — מסמן "בונה", לא "שופט"
  campaign-creation.prompt.js
  campaign-creation.types.js       ← ExtractedFacts, Brief
  extractors/
    free-text.extractor.js
    website.extractor.js
```

ר' `hamonym-backend/CLAUDE.md` — נוספה שם הקונבנציה הזו כתיעוד ריפו-רחב, כי היא רלוונטית לכל יכולת LLM עתידית, לא רק לתכונה הזו.

---

## החלטה 12 — כלל עסקי חד-משמעי נאכף בקוד, לא רק ב-Prompt

**Pipeline Milestone (2026-07-22): Facts → Brief validated.** Sprint 2 (`brief.builder.js`) חשף דפוס ששווה לקבע כעיקרון כללי לכל שאר ה-pipeline, לא רק למקרה הבודד שבו נמצא: כש-`ExtractedFacts.suggestedTargetAmount` הוא `null`, אסור שההצעה הסופית ב-Brief תכיל סכום בשום מקרה — לא משנה כמה טוב ה-prompt מנוסח.

**החלטה:** לכל שדה שיש לו כלל עסקי חד-משמעי וניתן לבדיקה תכנותית (לא שיפוט/יצירתיות) — האכיפה חייבת לקרות **בקוד**, אחרי קבלת תשובת ה-LLM, לא רק בבקשה ב-prompt. ב-`brief.builder.js` זה מיושם כ-guard מפורש: אם ל-Facts אין סכום, `suggestedTargetAmount.value` נכפה ל-`null` בקוד ללא תלות במה שה-LLM החזיר בפועל — לא רק "בקשה נחמדה" ב-prompt.

**למה זה עקרון כללי, לא רק תיקון נקודתי:** prompt-level guidance יכול להיכשל (Sprint 1 הוכיח את זה עם ה-URL וה-hallucination בקטגוריה) — אבל schema/code-level guarantee לא יכול. זה אותו עיקרון בדיוק כמו החלטה 7 (מידע רגיש מודר ברמת סכימה, לא רק מדיניות), רק מוכלל עכשיו לכל שדה עם כלל עסקי בינארי/בר-בדיקה, לא רק לשדות רגישים. מועמדים ברורים ליישום דומה בהמשך: `organizationNumber` (המשך לדרוש אישור ידני, ר' §5/§6, ללא תלות ב-confidence), נוכחות website/לוגו.

**איך זה מתבטא בפועל:** ה-LLM מציע (Prompt) → הקוד אוכף (Code). לא הפוך.

---

## החלטה 13 — המלצת AI בלי בית טבעי ב-Draft נשארת ב-Brief, לא נדחפת ל-Domain Model

**Pipeline Milestone (2026-07-22): Brief → Draft mapping validated (`draft.builder.js`, Sprint 3 — דטרמיניסטי, ללא LLM).** המיפוי חשף ש-`suggestedTone` ו-`suggestedHero` (Brief) אין להם שום שדה מתאים לא ב-`CampaignDraft` ולא ב-`OrganizationRegistrationState`.

**החלטה:** לא מוסיפים שדות חדשים ל-`CampaignDraft` כדי "לתת בית" להמלצות האלה. הן נשארות ב-Brief בלבד, מוחזרות תחת `unmapped` במקום להידחס לשדה שלא מתאים (כמו `heroCustomHtml`, שהוא HTML גולמי למשתמשים מתקדמים — לא משפט תיאורי).

**למה:** `CampaignDraft` הוא מודל של **מה שהקמפיין הוא** (state). Tone/Hero suggestions הן **המלצות AI זמניות** — קטגוריה שונה לגמרי, ולא צריכה להתערבב עם ה-state הקבוע. אם משתמש מאשר המלצת Hero בעתיד (במסך Brief Review שטרם נבנה), היא מתורגמת אז לתוכן קונקרטי בשדות הקיימים (למשל `shortDescription`/`heroTextStyle`) — ולא נשמרת כ"המלצה" בפני עצמה בתוך ה-Draft. זהו יישום נוסף של החלטה 4/9 (לא בונים סכימה לצרכן שאין לו עדיין UI קונקרטי).

## פער מוצר ידוע (לא נפתר, מתועד לצורך מעקב) — `targetAmount: 0`

`CampaignDraft.targetAmount` הוא `number` חובה, ברירת מחדל `0`. Brief's `suggestedTargetAmount.value` יכול להיות `null` ביושר (אין בסיס להציע סכום — ר' החלטה 12). המיפוי כופה `null → 0`, מה שאומר של-`0` יש כיום **שלוש** משמעויות שלא ניתנות להבחנה: "לא הוזן", "AI לא הצליח להסיק", "המשתמש רוצה יעד 0 בפועל". זו בעיית **Domain Model** של הפרונטאנד הקיים, לא באג בפייפליין ה-AI — לא בטווח של Sprint 3, ואינו נפתר כאן. מתועד לצורך מעקב עתידי (כשיבנה מסך Brief Review בפועל, יהיה צריך להחליט אם `targetAmount` הופך ל-`number | null` בקוד ה-Angular).

---

## שינוי דגש בין הספרינטים — מתועד לצורך המשכיות

Sprint 1-2 בדקו את ה-AI עצמו (איכות Extraction/Brief). Sprint 3 היה הראשון שבו ה-AI שימש בעצם כ**מבחן מאמץ** למודל המוצר הקיים — וחשף שני פערים אמיתיים ב-`CampaignDraft`/`OrganizationRegistrationState` שלא נבעו מבעיית AI כלל (ר' למעלה). זה סימן שה-pipeline עצמו התייצב: השאלות הפתוחות הבאות כנראה יהיו יותר ב-Domain Model/UX, פחות במנגנון ה-AI.

---

## Pipeline Milestone (2026-07-22): Website Extractor validated

Sprint 4 — לפי MVP §10. `website.extractor.js` הוא **Adapter דק**, לא Extractor שני עם לוגיקה כפולה: `Fetch → Readability (עם fallback ל-body text גולמי אם Readability מחזיר תוכן דל) → אותו FreeTextExtractor בדיוק`. אפס שכפול prompt/LLM logic.

**קריטריון הקבלה שהוגדר מראש (MVP §10) — נבדק ישירות**: אותו ארגון (`health-ambulance`) הורץ גם דרך ✍️ Free Text (Sprint 1) וגם דרך 🌐 Website (fixture HTML עם אותו תוכן עסקי, עטוף ב-nav/footer/aside אמיתיים) — Readability הצליח לנקות את כל ה-noise (ניווט, footer, זכויות יוצרים, widgets צדדיים) והשאיר רק את התוכן האמיתי. תוצאה: `organizationName`, `organizationNumber`, `suggestedTargetAmount`, `contactEmail`, `contactPhone` **זהים** בין שתי הריצות. פער אחד נמצא ולא תוקן באופן עיוור: `categoryGuess` יצא ריק בגרסת ה-Website מול `["חירום","רפואה"]` בגרסת ה-Free Text (ניסוח שונה מעט בין שני הטקסטים, לא bug מובהק ב-Extractor) — מתועד כפער איכות פתוח, לא שיפור פרומפט "עיוור".

**אימות אבטחה (MVP §8) — נבדק מול רשת אמיתית, לא רק תיאורטית**: SSRF guard חוסם בהצלחה `localhost`/`127.0.0.1` **לפני** כל קריאת LLM (נכשל על resolve/בדיקת IP, לא מגיע בכלל ל-fetch); `file://` ופרוטוקולים לא נתמכים נדחים מיד; URL לא תקין נכשל מיד. Smoke test מול אתר אמיתי (ויקיפדיה עברית) עבר מקצה לקצה — fetch, ניקוי Readability (~10.8K תווים תוכן נקי), extraction — בלי קריסה.

**אימות "Weak Result → Fallback" (הוספה שנקבעה לפני כתיבת קוד, MVP §10)**: fixture של landing page ללא תגיות סמנטיות (`<article>`/`<main>`) גרם ל-Readability להחזיר תוכן דל כצפוי — ה-fallback ל-body text גולמי תפס את התוכן האמיתי (61 תווים) והזרים אותו ל-Extraction בהצלחה, במקום כישלון גורף.

**תלות חדשה**: `@mozilla/readability` + `jsdom` (מוצמד ל-`^24.1.3` — גרסאות חדשות יותר של jsdom תלויות ב-ESM שלא תואם ל-CommonJS `require()` בגרסת Node הנוכחית של הפרויקט; אומת ידנית).

---

## Pipeline Milestone (2026-07-22): מחובר ל-UI אמיתי, נבדק עם משתמש אמיתי

**Sprint 5 — המסך הראשון.** `/campaigns/create/ai` (Angular) + `POST /api/campaign-creation/extract` (Express, `src/modules/campaign-creation/`) — תרשים הכניסה (החלטה 8), קלט טקסט/אתר, תצוגת Brief מלאה עם reason לכל שדה. זה השלב שהופך את ה-pipeline המוכח ל"תכונה שמשתמש יכול לגעת בה".

**באג אמיתי נמצא ותוקן דרך שימוש חי, לא רק corpus**: `[(ngModel)]` נקשר ישירות ל-signal (`input = signal('')`) — קשירה שגויה שמחליפה בשקט את ה-signal במחרוזת רגילה בהקלדה ראשונה, וקוראסת בקריאה הבאה ל-`input()`. תוקן ל-`[ngModel]="input()"` + `(ngModelChange)="input.set($event)"`.

**באג שני, יותר משמעותי, נמצא רק אחרי דיבאג ארוך**: "הספינר לא מפסיק להסתובב" לא היה קשור בכלל ל-AI, ל-CORS, או ל-backend (כולם אומתו תקינים בנפרד) — `AppComponent` משאיר בכוונה overlay טעינה גלובלי פתוח אחרי ניווט לכל route שמתחיל ב-`/campaigns` (`SELF_HIDING_PREFIXES`), ומצפה שהעמוד היעד יסגור אותו בעצמו אחרי טעינת הנתונים שלו. המסך החדש לא עשה זאת (אין `ngOnInit`) — ה-overlay חסם כל קליק על העמוד מרגע הניווט, לפני שהמשתמש בכלל נגע בכפתור. **נמצא ואומת רק דרך שחזור מלא עם Playwright** (session מחובר אמיתי, לא רק בדיקות שרת) — לקח מהותי: כש-curl/בדיקות שרת כולן עוברות אבל המשתמש עדיין רואה תקלה, הבעיה כנראה ב-UI layer שלא נבדק ישירות.

**Sprint 6 — Document Collection.** נבנה **מיד** אחרי Sprint 5 לפי בקשה מפורשת של המשתמש (ר' "סטייה מודעת" ב"מצב נוכחי" למעלה) — לא אחרי ולידציה עם משתמשים אמיתיים כפי שהעיקרון "צוואר הבקבוק" קבע. תמיכה בתמונות (Vision — לוגו, תמונות פעילות, מסמכים מצולמים), PDF טקסטואלי (`pdf-parse`), Word (`mammoth`). PDF סרוק (ללא שכבת טקסט) מזוהה ומסומן כלא-נתמך במפורש במקום כישלון שקט — עקיפה: להעלות כתמונה במקום. אימות אמיתי: תמונה שנוצרה בפועל (טקסט מצויר עם `System.Drawing`) עברה דרך ה-API האמיתי של OpenAI Vision וחילצה נכון מספר עמותה/שם/תיאור; PDF נבדק עם mock (הספרייה עצמה לא נבדקה מחדש — ספרייה בוגרת), Word נבדק מול fixture אמיתי מ-test suite של mammoth עצמה.

**תלויות חדשות נוספות**: `pdf-parse` (מוצמד ל-`^1.x` — אותה בעיית Node<20/ESM כמו jsdom, גרסה 2 קרסה), `mammoth`. `llm.service.js`'s `complete()` כבר תמך ב-multi-modal content arrays (תמונות) בלי שום שינוי — `userPrompt` תמיד הועבר ישירות ל-`content` של הבקשה ל-OpenAI.

---

## Pipeline Milestone (2026-07-22): קלט משולב + יצירת קמפיין אמיתי מקצה לקצה

**Sprint 7א — קלט משולב.** לפי בקשה מפורשת: טקסט/אתר/קבצים הפכו מ-3 טאבים בלעדיים לשלושה שדות שכולם אופציונליים, ניתן למלא כל שילוב (לפחות אחד נדרש). **לא** פותח מחדש את בעיית ה-Facts Merger שנדחתה — כי אין כאן חילוץ נפרד לכל מקור ומיזוג של תוצאות נפרדות; הכול מתאחד לטקסט/תמונות **לפני** קריאת LLM בודדת, בדיוק כמו ש-Document Collection כבר עשה לכמה קבצים. `website.extractor.js` פוצל: `fetchMainContent(url)` (fetch+נרמול בלבד) נחשף בנפרד מ-`extract()` (שעדיין עושה גם את קריאת ה-LLM, למקרה השימוש הבודד). כשל fetch של אתר לא מכשיל את כל הבקשה — מצוין, וההגשה ממשיכה עם מה שכן סופק (טקסט/קבצים).

**Sprint 7ב — יצירת קמפיין אמיתי + מעבר לסטודיו.** לפי בקשה מפורשת: "אחרי שיוזר בונה דרך AI, הוא אחר כך יכול לערוך את זה דרך ה-Wizard". נחשף `POST /api/campaign-creation/map-to-draft` (Sprint 3's `draft.builder.js`, סוף סוף מחובר ל-HTTP). כפתור "אשר וצור קמפיין" חדש במסך ה-Brief: ממפה ל-patches → `CampaignStudioStateService.reset()+patch()` → `CampaignApiService.create()` (**שימוש חוזר** בשירותים הקיימים של הסטודיו, לא HTTP calls חדשים בעברית-סנייק ידנית) → ניווט אוטומטי ל-`/campaigns/:id/edit`.

**החלטת סקופ (2026-07-22, מפורשת)**: מחובר **רק** לעמותה הקיימת של המשתמש (`CurrentEntityService`) — יצירת עמותה **חדשה** מתוך Brief נדחתה במפורש לשלב אחר, לא נבנתה כאן. הגעה לדף הזה כבר דורשת תפקיד entity-manager (`campaignEditorGuard`), כך שהמקרה הנפוץ (עמותה קיימת) מכוסה.

**אימות אמיתי, לא רק "זה בונה"**: Playwright מקצה לקצה (הזנה → Brief → אישור → `POST /api/campaigns` בפועל → ניווט לסטודיו) הורץ פעמיים, כולל **שאילתת DB ישירה** על השורה שנוצרה לאימות שהשדות (title, short_description, target_amount, hero_cta_config עם visible:true) אכן נשמרו נכון תחת ה-entity_id האמיתי. ממצא כן אבל: עם קלט שנוסח כ"עמותת נסיון לבדיקה" (מטא-התייחסות לבדיקה עצמה), ה-AI נמנע במכוון מלהמציא כותרת/תיאור — נבדק שוב עם טקסט "אמיתי" (עמותת לב אחד/אמבולנס) והשדות התמלאו נכון; לא באג. ממצא נוסף: `category` יצא `null` בהרצה זו למרות שטקסט דומה בעבר הפיק קטגוריה — הנתיב המשולב עוטף טקסט חופשי ב"טקסט חופשי מהמשתמש:\n" לפני החילוץ, ומכיוון שה-temperature הוא 0 (דטרמיניסטי לפי קלט מדויק), עטיפה כזו יכולה לשנות תוצאה — שונות איכות קלה, לא באג בחיווט.

---

## Pipeline Milestone (2026-07-23): באגים אמיתיים שנמצאו רק דרך שימוש חי, ותכונות שנבנו לפי בקשה מפורשת

מכאן ואילך הפער בין "עובד ב-corpus/fixtures" ל"עובד בשימוש אמיתי" הפך למקור הראשי לממצאים — כל הפריטים הבאים נמצאו על ידי המשתמש עצמו תוך כדי שימוש אמיתי באפליקציה, לא דרך בדיקות מתוכננות מראש.

**קישור YouTube לא הפך ל-Hero**: `ExtractedFacts` שם כל URL תחת `socialLinks` בלבד, ו-`socialLinks` מעולם לא עברה מ-Facts ל-Brief (נשמטה בשקט). תוקן: שדה ייעודי `heroVideoUrl` (מזוהה ב-regex ל-youtube.com/youtu.be/vimeo.com, לא רק נסמך על ה-LLM), מועבר כשדה רגיל ב-Brief, וממופה ב-`draft.builder.js` ל-`heroType:'video'` + `videoUrl`. **אימות ראשון היה false positive**: נבדק רק דרך `node -e` ישיר על ה-pipeline (process טרי, קוד עדכני תמיד) ודווח כתקין — המשתמש דיווח מיד שזה עדיין לא עובד בפועל. אבחון מחדש מול ה-HTTP endpoint האמיתי גילה ש-port 3000 הוגש על ידי **process ישן ובודד** (`node src/server.js` בלי nodemon) ששירת קוד ישן בלי לדעת. לקח קבוע: לפני שסומכים על בדיקה חיה, לבדוק מי מחזיק את ה-port בפועל (`nodemon` תקין מול process בודד שלא נטען מחדש).

**לוגו/תמונת Hero לא הגיעו לקמפיין**: קבצים שהועלו שימשו רק כקלט ל-Vision LLM לחילוץ טקסט ונזרקו לאחר מכן — אף תמונה לא הגיעה בפועל לקמפיין שנוצר. תוקן: שימוש חוזר ב-`UploadService`/`/api/media/upload` (אותה תשתית שצעד המידע הבסיסי הידני של הסטודיו כבר משתמש בו) — קובץ מתויג "לוגו" → `campaignLogoUrl`, התמונה הראשונה האחרת → `coverImageUrl`, אלא אם כבר נקבע `heroVideoUrl` (וידאו מנצח תמונה שהועלתה). כשל העלאה לא חוסם יצירת קמפיין.

**זיהוי `entityType` לא אמין**: המשתמש כתב "זה עמותה" במפורש ועדיין התבקש לבחור סוג ידנית. שורש הבעיה: `entityTypeGuess` היה ברשימת השדות בפרומפט בלי שום כלל מפורש מתי למלא אותו — לכל שדה אחר היה כלל ממוספר, לזה לא. נוסף כלל מפורש עם רשימת המונחים המוכרים (עמותה/חל"ץ/עוסק פטור/עוסק מורשה/מפלגה). **חזר שוב** בנתיב הקלט המשולב (טקסט+אתר+קבצים ביחד) — כנראה בעיית תשומת-לב של ה-LLM עם נפח טקסט גדול יותר, לא בעיית ציות לכלל. נוסף Backstop דטרמיניסטי: סריקת מילות-מפתח ב-Regex בקוד עצמו (`detectEntityTypeFallback` ב-`free-text.extractor.js`), משמש רק כשה-LLM מחזיר null.

**"צור רק עמותה, בלי קמפיין" — נבנה, ואז עוצב מחדש באותו יום**: גרסה ראשונה הייתה checkbox מקונן תחת "צור עמותה חדשה" + זיהוי אוטומטי מטקסט חופשי ("רק עמותה"). המשתמש מצא את ה-UID הדו-שכבתי לא ברור וביקש בחירה תלת-כיוונית ישירה. עוצב מחדש ל-selector בודד ומפורש (`creationMode: 'campaign'|'org-only'|'org-and-campaign'`) עם 3 אפשרויות מסומנות בבירור, מוצג **לפני** שה-Brief אפילו נוצר. הזיהוי האוטומטי מטקסט חופשי **הוסר** בשלב הזה — מקור-אמת שני היה מתנגש עם selector מפורש המוצג ממש מעליו.

**קבצי עמותה (לוגו/תעודת התאגדות) לא הגיעו בכלל**: תיקון קודם כיסה רק תמונות ברמת קמפיין; קבצים ברמת עמותה בזרימת "צור עמותה חדשה" עדיין נשמטו בשקט. תוקן דרך אותם endpoints שהאשף הידני כבר משתמש בהם (`EntitiesService.uploadLogo`/`uploadAssociationDocument`).

**פסקת סיפור ארוכה יותר + טקסונומיית קטגוריות אמיתית**: המשתמש בחן Brief אמיתי והצביע על שני פערים. (1) רק `shortDescription` בת 160 תווים הייתה זמינה לגוף עמוד הקמפיין — הרחק מסיפור אמיתי. נוסף `Brief.story` (~150-350 מילים, מבוסס אך ורק על מקורות שכבר קיימים ב-Facts, אותה משמעת "לעולם לא להמציא" כמו כל שדה אחר, `null`+reason כשאין מספיק חומר). (2) `categoryGuess` הייתה טקסט חופשי שה-LLM המציא, לא מאומת מול כלום, ושמרן מדי (למשל "קייטנת קיץ לילדים חולי סרטן" לא מופה ל"בריאות" בלי המילה המפורשת). הוחלף ברשימת 31 הקטגוריות האמיתיות והקבועות של המערכת (`entity-categories.ts` — אותו מקור-אמת שהבורר הידני משתמש בו), עם הרשאה מפורשת בפרומפט להסיק בביטחון מתוכן תמטי ברור.

**קונפליקט cache של entity אחרי יצירת עמותה+העלאת קבצים**: `switchContext()` נקרא מיד אחרי יצירת עמותה, מה שהפעיל את ה-effect הרקעי של `CurrentEntityService` — אותו fetch התחרה מול העלאות הלוגו/תעודה שעדיין רצות ויכול היה "לנצח", ולשמור snapshot טרום-העלאה ב-cache. `entity-settings.component.ts` קורא את ה-cache הזה **פעם אחת** ב-`ngOnInit` (לא binding חי), אז מרוץ מפסיד תקע את העמוד ב"לא הועלה" לצמיתות. תוקן ברצף מפורש: העלאה → fetch מחדש של העמותה → `setEntity()` עם נתונים טריים → `switchContext()` → ניווט.

**מצב "עמותה בלבד" עדיין הציג את כל מדור הקמפיין**: עטיפת כל `.brief-section` ב-`@if (!orgOnly())`.

**שימוש חוזר ב-`registration_number` אחרי מחיקה — באג אמיתי, לא קשור ל-AI, נמצא תוך כדי בדיקה חיה**: soft-delete של עמותה מעולם לא שחרר את `registration_number` שלה לשימוש חוזר — ה-DB עדיין החזיק `UNIQUE(registration_number)` גורף מלפני שנוסף soft-delete. תוקן ב-migration 029: אינדקס `UNIQUE` חלקי (`WHERE deleted_at IS NULL`) במקום הגורף.

**רגרסיה נוספת שנתפסה באותה בדיקה חיה**: `applyStoryContent()` תמיד קרא את `brief.organizationDescription` הגולמי, והתעלם משינוי שהמשתמש ביצע ב-`newOrgDescription` בעמוד ה-Brief — כך שתיאור שמולא ידנית (שימוש צפוי ולגיטימי בשדה) מעולם לא הגיע לקמפיין. תוקן להעדיף את הגרסה (האולי-ערוכה) כש-`createNewOrg` פעיל.

**"צור עמותה" — סקופ שנדחה במפורש בעבר, נפתח מחדש לפי בקשה ישירה**: המשתמש ביקש שה-AI ייצור עמותה חדשה בפועל, "יכניס את המשתמש אליה", ויצור קמפיין תחתיה. עיצוב משותף עם המשתמש: יצירת עמותה מופעלת ב-**toggle מפורש** במסך הכניסה, לא זיהוי-כוונה מ-LLM על טקסט חופשי — יצירת ישות אמיתית ב-DB הרגישה מספיק כדי לדרוש פעולה מכוונת, לא ניחוש. המימוש עושה שימוש חוזר ב-`organization-registration-state.service.ts`'s `buildPayload()`/`initialState` — עמותה שנוצרה על ידי AI זהה שדה-לשדה לעמותה שנרשמה ידנית. `organizationNumber`/`entityType` הם 2 מתוך 3 שדות ש-`canSaveDraft()` דורש ו-Brief בדרך כלל לא יכול לספק את שניהם — כפתור האישור נשאר מנוטרל עד שהמשתמש ממלא את שניהם, לפי דרישה מפורשת: "זה חייב להיות אינטראקטיבי". תוך כדי המחקר לקראת הבנייה התגלו **שתי מערכות מעקב-context מקבילות** (`CurrentEntityService` הישן ו-`CurrentContextService` שהוא מקור-האמת האמיתי של המתג) — ותהליך ההרשמה הידני עצמו סבל מאותו פער (עמותה שנרשמה ידנית לא מופיעה במתג ה-topbar עד login מחדש). נוסף `CurrentContextService.addEntityContext()`, שמתקן את הפער הזה גם עבור הזרימה הידנית, לא רק עבור AI.

## Pipeline Milestone (2026-07-23): AI כ-Creative Director, שאלות הבהרה, ו-Generation Logging

שלושה תוספים משמעותיים לאותה קריאת Brief אחת — פירוט מלא, כולל מה נדחה ולמה, ב-[AI_CREATIVE_DIRECTOR_CONCEPT.md](AI_CREATIVE_DIRECTOR_CONCEPT.md): **Gallery Curation** (בחירת Hero/סדר גלריה/הסתרת תמונות מבוססת Vision, עם ולידציה מחדש בקוד), **Design Intent** (emotion/urgency/focus/energy — לא צבעים/Template, זו החלטת מערכת), **Content Strategy** (Metadata פנימי, לא מוצג למשתמש). אותו מסמך גם מתעד את המסגור המחדש הקונספטואלי "Brief = Campaign Specification v1" (לא Rename בקוד).

**שאלות הבהרה (clarifying questions)** — תוסף ארכיטקטוני אמיתי אך מוגבל בכוונה: המשתמש שיתף קמפיין אמיתי שפורסם (`hamonym.com/campaign/...`) כאמת-מידה — עם פרטים תפעוליים ספציפיים ביותר (מספר משתתפים מדויק, תאריכי מחנה, עלות לילד, מיקום) שאף Extraction/Web Search לא יכולים להמציא. הפתרון שהתגבש: ה-AI מזהה בדיוק מה חסר ושואל 2-5 שאלות ממוקדות, כותב את הסיפור האמיתי אחרי שנענה. נבנה כזרימה **stateless בת שני requests, לא session רב-שיחות** (עדיין נאמן להחלטה 2 — אין Agent, אין זיכרון) — ה-Backend חסר זיכרון בין הסבבים לגמרי, הפרונטאנד רק מעביר את ה-`facts` הלאה ב-signal. שדה חדש `Brief.clarifyingQuestions`; endpoint חדש `POST /api/campaign-creation/refine-brief`. מוגבל לסבב אחד — אחרי ש-`userAnswers` כבר סופקו, לעולם לא יוחזרו עוד שאלות. אומת מקצה לקצה: קלט דליל במכוון קיבל 5 שאלות ממוקדות נכון; מענה עליהן הפך פסקה גנרית בת 469 תווים לפסקה שמזכירה תאריכים/מספר משתתפים/עלות/מיקום מדויקים, ברמה שתואמת את הקמפיין האמיתי שהמשתמש שיתף.

**Generation Logging** — אחרי דיון ארכיטקטורה ארוך (ר' "AI Architecture" למטה), המשתמש ביקש את הצעד הקטן והבטוח ביותר: לשמר כל generation של Brief כארטיפקט בלתי-הפיך. טבלה חדשה `campaign_ai_generations` (migration 031) — שורה אחת לכל generation, `campaign_id` מקושר בדיעבד (לא כל generation מגיע לקמפיין אמיתי, וזה בסדר), `prompt_version`+`model` נשמרים כדי שלשאלות עתידיות מסוג "למה התוצאות השתנו" תהיה תשובה אמיתית. Logging הוא **best-effort בכל מקום** — כישלון logging לעולם לא שובר את זרימת יצירת הקמפיין עצמה. אומת: שורה אמיתית נרשמה עם השדות הנכונים, קושרה נכון לקמפיין שהיא הפיקה.

## Pipeline Milestone (2026-07-23): חיפוש אינטרנט אמיתי

המשתמש שאל במפורש "אתה לא יכול פשוט לחפש באינטרנט?" ודחה נכון הנחה קודמת שנדרש מנוי API חיפוש נפרד. מתברר של-OpenAI עצמו יש כלי `web_search` דרך ה-Responses API (`client.responses.create({tools:[{type:'web_search'}]})`), עם אותו `OPENAI_API_KEY` הקיים. נבנה `organization-research.tool.js` — לוקח שם/אתר עמותה, מחזיר טקסט מבוסס + מקורות מצוטטים אמיתיים דרך `llmService.completeWithWebSearch()` חדש. Opt-in מפורש בלבד (`enableWebResearch`, כבוי כברירת מחדל) — עלות/latency אמיתיים לכל קריאה. אומת חי: הסיפור קלט עובדה אמיתית (מספר מוטבים 2022) שהופיעה רק בדפים שנסרקו, לא בטקסט המקורי של המשתמש, עם מקורות מצוטטים נכון.

חמש נקודות משוב שהמשתמש נתן על הפיצ'ר טופלו באותו יום: קישורי מקור מקופלים תחת `<details>/<summary>` במקום רשימה גלויה; **cache אמיתי** (`organization_research_cache`, TTL של 30 יום, נבדק 20 שניות קריאה קרה → כ-300ms פגיעה ב-cache); "recency guard" בפרומפט (רק נתונים מ-2-3 השנים האחרונות, עם שנה מצוינת); ניסוח מחדש של ה-checkbox מ"חפש באינטרנט" ל"✨ העשר את התוכן במידע ציבורי על העמותה". נקודה חמישית — פיצול קריאת ה-Web Search לעובדות JSON נפרדות מהפרוזה, לשימוש חוזר בסוכנים אחרים — **נבדקה ונדחתה במפורש**: שילוב `web_search` עם `json_schema` קשיח באותה קריאה פוגע מדידה ביכולת ה-grounding (ציטוטים נעלמים, מספרים הופכים גנריים). מתועד בהערות הקוד עצמו, לא נבנה.

## Pipeline Milestone (2026-07-24): איכות טקסט ה-Story, שילוב תמונות בתוך הטקסט, ומתודולוגיית Prompt Engineering

המשתמש דיווח משתי בעיות אמיתיות אחרי שימוש: (1) הטקסט שנוצר "רזה" למרות שכל התשתית (חיפוש אינטרנט, שאלות הבהרה) כבר קיימת; (2) תמונות שהועלו נחתו בבלוק גלריה נפרד במקום להשתלב עם הטקסט.

**תיקון ראשון — כלל "שם תוכנית ספציפי חובה"**: הכלל הקודם התיר אך לא **חייב** שימוש ב-OnlineResearch, אז המודל יכול היה לבחור בעובדות ה"קלות" (שנת ייסוד, מספר כללי) גם כשעובדות ספציפיות יותר (שם תוכנית) היו זמינות. שונה לחובה מפורשת: לפחות 3-4 פרטים קונקרטיים, לפחות אחד מהם שם תוכנית/פרויקט בשמו. אומת: אותה עמותה עברה מציון עובדות גנריות לציון שמות תוכניות אמיתיים ('גן החלומות', 'טיסת החלומות', ובחלק מהריצות גם 'רכבת החיוכים בפורים').

**ניסוי מתודולוגי מבוקש במפורש על ידי המשתמש**: להדביק דוגמת קמפיין אמיתית מצוינת לפרומפט ולהשוות עם/בלי. תוצאה: דוגמה בודדת **לא** שינתה את הפתיחה באופן עקבי — המודל המשיך לפתוח ב"עמותת X פועלת למען..." הגנרי, בדיוק הדפוס שהדוגמה נועדה להדגים הימנעות ממנו. במקום זאת, **כלל מבני מפורש** ("המשפט הראשון חייב לתאר סיטואציה אנושית קונקרטית, לא להציג את העמותה") עבד בעקביות על פני שני תחומי קמפיין שונים לגמרי (עמותת ילדים חולי סרטן, קמפיין ממ"ד קהילתי). הממצא הכללי, לא הצר: **כלל מבני מפורש עדיף על דוגמה בודדת** לכיוון gpt-4o-mini הרחק מדפוס ברירת מחדל עקשן — עשוי לחול על כל מנוף איכות עתידי (כותרות, סדר פסקאות), לא רק על משפט הפתיחה. שני לקחים נלווים: לעולם לא לצטט את הביטוי האסור המדויק בתוך הפרומפט (המודל נוטה להדהד בדיוק את מה שנאמר לו לא לומר — נמצא כשניסוח שלילי לכלל ה-CTA גרם למודל לחזור על הביטוי המדויק שנאסר); ולפני שקובעים ששינוי פרומפט "תיקן" משהו, להריץ יותר מפעם אחת — השוואת ריצה-בודדת רועשת מספיק כדי להטעות, כפי שקרה כאן עם ה-CTA (שיפור אמיתי אך לא-עקבי, שונות גבוהה בין ריצות זהות).

**`interleaveStoryWithImages()` — אומת חי מקצה לקצה**: הפונקציה (שהייתה כתובה מסבב קודם, לא נבדקת) מפצלת את הסיפור לפסקאות, כל פסקה בבלוק `rich-text` נפרד, עם תמונות גלריה משולבות ביניהן. אומת לא רק ב-DB אלא בדפדפן אמיתי (עורך → רשימת בלוקים → תצוגה מקדימה ציבורית אמיתית של דף התרומה). **באג אמיתי נמצא ותוקן**: כל בלוק מפוצל ירש את ה-`label` של הבלוק המקורי, אז הכותרת "על המיזם" חזרה על עצמה פעם לכל פסקה. תיקון ראשון (label ריק) לא החזיק — `campaign-api.service.ts`'s נורמליזציית טעינת בלוקים (`b.label || DEFAULT_BLOCK_LABELS[b.type] || ''`) מחליפה label ריק בברירת מחדל גנרית ("טקסט") בכל טעינה מה-API, אז הכותרת השגויה חזרה. נפתר עם label של רווח בודד (' ') — אמיתי מספיק כדי לדלג על ברירת המחדל, בפועל לא-נראה.

**אימות מוכנות הנתונים למדידה עתידית**: לפני סגירת הסבב, נבדק אם `campaign_ai_generations.brief_json` (ההצעה המקורית של ה-AI) מספיק כדי להשוות בעתיד מול מה שבאמת פורסם — שאלות כמו "כמה % מהמשתמשים משכתבים את ה-CTA". נמצא: השוואת CTA היא ישירה (`suggestedCtaLabel.value` מול `hero_cta_config.label`, אימות על שורה אמיתית ב-DB), אך השוואת story דורשת נרמול — `interleaveStoryWithImages()` מפצל את הטקסט למספר בלוקים עם HTML escaping, אז השוואת מחרוזת גולמית תיתן false positive ("המודל שינה הכול") גם כשהמשתמש לא נגע בכלום. תועד כפרט טכני לזכור, לא נבנה (אין עוד עבודת AI מתוכננת כרגע — ר' "מה הלאה" למטה).

**נקודת עצירה מפורשת (2026-07-24)**: אחרי הסבב הזה, המשתמש ביקש במפורש לא להמשיך לשפר עוד את ה-CTA, לא לבנות "ספריית השראה" של קמפיינים לדוגמה, ולא להוסיף יכולות AI נוספות — לתת למערכת להיתקל בשימוש אמיתי ולאסוף נתונים, ולחזור לשיפור ה-AI רק כשתהיה אינדיקציה אמיתית איפה צוואר הבקבוק הבא.

---

## קוד קיים שרלוונטי (התגלה 2026-07-22 בבדיקת הקוד, לא הונח מראש)

- **`hamonym-backend/src/agents/approval/`** — ApprovalAgent אמיתי, פועל: `approval.agent.js`/`.facts.js`/`.checks.js`/`.prompt.js`/`.types.js` + `tools/` (entity, campaigns, document, guidestar, websearch). מפיק `ApprovalCheck[]` עם `status: 'pass'|'warning'|'fail'` — קרוב מאוד למה שהחזון (למטה) דמיין בתור "Readiness Check". יש לבדוק לעומק לפני שבונים Readiness חדש — אולי זה כבר קיים בפועל.
- **`hamonym-backend/src/agents/campaign-advisor/`** — CampaignAdvisorAgent אמיתי, פועל: `advise()` ו-`generateMetadata()`. יש לו `CampaignFacts` type משלו (שלמות קמפיין קיים — booleans/counts כמו hasTitle/hasHeroImage/supportersCount) — **שונה** מ-`ExtractedFacts` שלנו, ומכאן ההתנגשות שגרמה לשינוי השם בהחלטה 4.
- **`hamonym-backend/src/agents/llm.service.js`** — wrapper גנרי סביב OpenAI (`gpt-4o-mini`), `complete(systemPrompt, userPrompt) → parsed JSON`. משותף לכל ה-agents, כולל campaign-creation העתידי.
- **`hamonym-backend/src/agents/approval/guidestar.service.js`** — אינטגרציה אמיתית מול guidestar.org.il (מרשם עמותות ישראלי). רלוונטי ישירות לבעיית "`organizationNumber` לא אמין מ-Extraction" — פוטנציאל לאימות מספר עמותה מול מקור רשמי, במקום לסמוך על ניחוש LLM בלבד. לא הוחלט עדיין אם/איך לשלב זאת ב-v1 — לבחון במסמך ה-MVP.

---

## סקופ MVP (Extraction v1)

מוגבל במכוון:

- מקור קלט **אחד** בכל הרצה (אתר **או** מסמך **או** טקסט חופשי) — לא שילוב של כמה מקורות יחד.
- **לא** נתמך ב-v1: Canva, Dropbox, Facebook, Instagram, YouTube, OCR, הודעות קוליות.
- AI Creator פועל **רק** על עמותה קיימת ומאושרת, או יוצר טיוטת עמותה חדשה (`status: draft`) — לעולם לא "מקים עמותה" מול רשם העמותות בפועל.

## MVP v1 — Extractors בגל הראשון: Free Text + Website

שני מקורות קלט ב-release הראשון למשתמשים אמיתיים (לא רק Free Text):

- **✍️ טקסט חופשי** — נבנה ראשון (Sprint 1), מוכיח את כל ה-pipeline מקצה לקצה בלי תלות בתשתית extraction חיצונית.
- **🌐 אתר אינטרנט** — נבנה שני (Sprint 2), על גבי אותו Facts/Brief. זו יכולת ה-"wow" שמדגימה את הערך הייחודי של המוצר ("תן לי את האתר שלך → 30 שניות → הנה הקמפיין"), ולכן שייכת ל-v1 גם אם free text מספיק כדי להוכיח את הארכיטקטורה.

**חשוב לתקצוב**: ה-Website Extractor אינו "עוד שלב קטן" מעל free text — הוא מכניס שטח כשלים ואבטחה משלו שלא קיים ב-free text בכלל: SSRF (יש לחסום fetch לכתובות פנימיות/private ranges), timeouts, אתרים לא נגישים, JS-rendered SPAs, robots/rate-limits. יש לתקצב אותו כ-scope גדול משמעותית מ-Sprint 1, לא כתוספת שולית. פרטי טיפול בכשלים אלה (Failure Strategy) שייכים למסמך ה-MVP.

**נדחים לגלים הבאים**: PDF, Word, OCR, Canva, רשתות חברתיות, הודעות קוליות.

---

## מה הלאה

מסמך זה **לא** כולל: רשימת שדות מפורטת, מסכי UI, פירוק למשימות פיתוח. אלה שייכים למסמך MVP נפרד ([AI_CAMPAIGN_CREATION_MVP.md](AI_CAMPAIGN_CREATION_MVP.md)).

---

## Vision (Non-binding) — כיוון עתידי, לא בסיס להחלטות

הסעיף הזה הוא מצפן לטווח ארוך, **לא** ארכיטקטורה מאושרת. שום דבר כאן לא גובר על ההחלטות 1–13 למעלה, ואין לתכנן סכימות/מודלים של ה-MVP כדי "להתאים מראש" למה שמתואר כאן.

החזון: המונים הופך בהדרגה מ"בונה קמפיינים" ל-**Campaign Creation Platform** — כמה שכבות יכולת, כולן פועלות מעל אותו `CampaignDraft`, בלי לפצל את הארכיטקטורה:

- 🧠 **AI Start** — יוצר Draft כמעט מוכן מתוך חומרי מקור (זה שכבר מתוכנן ב-MVP הזה).
- ⚡ **Quick Wizard** — הקמת קמפיין בכמה דקות, ללא AI.
- 🛠 **Campaign Studio** — עורך מתקדם, שליטה מלאה (קיים היום).
- 🔍 **Campaign Advisor** *(קיים בפועל, לא היפותטי — ר' "קוד קיים" למעלה)* — ייעוץ לשיפור קמפיין קיים על בסיס שלמות/ביצועים — מודל נתונים נפרד מ-`ExtractedFacts` (ר' החלטה 9).
- ✅ **Readiness Check** *(ייתכן שכבר קיים בפועל כ-ApprovalAgent — ר' "קוד קיים" למעלה; יש לבדוק לפני שבונים משהו נפרד)* — בדיקת מוכנות-לפרסום. אם נבנה כחלק נפרד, השם חייב להימנע מ-"Approval" — זה כבר מונח עסקי תפוס (`pending_review → approved`, בסמכות admin אנושי בלבד — ר' החלטה 6).
- 🗂️ **ריבוי מקורות + Facts Merger** *(רעיון עתידי, לא מתוכנן, לא MVP)* — במקום לבחור מקור קלט אחד (החלטה 8), המשתמש "זורק" כמה **סוגי** מקורות שונים ביחד (אתר + PDF + תמונות + טקסט + קישורי ספרייה/Drive), כל אחד עם Extractor משלו, ומעל כולם שכבת **Facts Merger** שמאחדת ל-`ExtractedFacts` אחיד עם מעקב מקור לכל שדה (`{value, source}`) וטיפול בקונפליקטים בין מקורות. נדחה במפורש (2026-07-22, שוב 2026-07-22 בבקשה נוספת לריבוי-קלטים+"קישור לספרייה"): **לא רק "שכבה גנרית חדשה" — שובר את ההנחה הבסיסית ביותר שכל הארכיטקטורה נשענת עליה: Extractor אחד → Brief אחד.** ברגע שיש כמה מקורות, נפתחות שאלות שאין להן תשובה מוכחת היום: מי מנצח בקונפליקט (PDF מול אתר)? האם עדכניות קובעת? האם ממזגים או מבקשים מהמשתמש לבחור? איך מסבירים למשתמש למה נבחר מקור אחד על פני אחר? זו לא הרחבה — זו מערכת החלטות עסקיות חדשה לגמרי. גם ה"קישור לספרייה" כשלעצמו לא מוגדר טכנית (Google Drive? Dropbox? רשימת קבצים גולמית? — כל אחד דורש אינטגרציה/הרשאות שונה). לשקול מחדש רק אחרי שיש לפחות שני Extractors אמיתיים בשימוש **וגם** ולידציה עם משתמשים אמיתיים על הזרימה הבסיסית (ר' עיקרון "צוואר הבקבוק" למטה).

- 📁 **Document Collection** *(הערת עיצוב עתידית ל-Document Extractor, לא MVP, לא לבנות עכשיו)* — **שונה במפורש מ"ריבוי מקורות" למעלה.** לא כמה *סוגי* מקור (אתר+טקסט+PDF), אלא כמה *קבצים* בתוך אותו ערוץ קלט "📄 ממסמך" — משתמש שמעלה "כל מה שיש לי על הארגון" (כמה PDF, Word, לוגו, תמונות ביחד), כי כך ארגונים באמת מחזיקים חומר: לא מסמך אחד שמספר הכול, אלא כמה חומרים משלימים. **הבחנה קריטית שחייבת להישמר כשזה ייבנה**: קלט = `DocumentCollection` (לא `DocumentFile` בודד), אבל עדיין **Extraction אחד** בעל משמעות עסקית — לא extraction-per-file ואז merge. מבחינה טכנית ייתכן נרמול נפרד לפי סוג קובץ לפני כן (PDF parser / OCR-Vision / וכו') אבל זה שלב טכני פנימי, לא שלב Facts נפרד ולא Facts Merger. כשייבנה בפועל Document Extractor, כדאי לתכנן את סוג הקלט שלו כ-`DocumentCollection` מראש (הרחבה כמעט חינמית באותו רגע) ולא כ-`DocumentFile` בודד שידרוש refactor מאוחר יותר.

**עיקרון מנחה**: לא לתת לחזון העתידי לעצב קוד של היום. כל שכבה עתידית תתוכנן ותיבנה כשתהיה לה הצדקה קונקרטית, לפי הבעיה שהיא פותרת בפועל — לא לפי ניחוש מראש. וכפי שהתגלה עכשיו: לפני שבונים "עתידי" — לבדוק שהוא לא כבר קיים.
