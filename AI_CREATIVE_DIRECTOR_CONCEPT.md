# AI כ-Creative Director — קונספט ומימוש

**תאריך:** 2026-07-23
**שייך ל:** `AI_CAMPAIGN_CREATION_VISION.md` (ה-Brief step הקיים) — מסמך זה הוא הרחבה ממוקדת של דיון ספציפי, לא מסמך עצמאי.
**מקור:** דיון חי שהתחיל משאלה פשוטה ("האם אתה מבקש מ-OpenAI שיעזור בעיצוב?") והתפתח דרך כמה סבבים לכדי מסגרת עבודה מלאה.

## הרעיון המרכזי

עד עכשיו ה-LLM שימש **ככותב תוכן**: כותרת, תיאור, סיפור, CTA. השאלה שעלתה — האם אפשר לקחת אותו צעד קדימה ולתת לו להיות **Creative Director** של הקמפיין, בלי לתת לו שליטה חופשית על עיצוב.

ההבחנה הקריטית שהתגבשה בדיון: **"תן ל-AI לעצב" לעומת "תן ל-AI להיות Director"**. ה-AI מקבל יותר החלטות יצירתיות, אבל **תמיד מתוך אוצר האפשרויות שהמערכת כבר תומכת בו** — הוא לא ממציא ערכים חדשים (צבע HEX, שם Template) שאחר כך צריך למפות למשהו אמיתי.

## שלושה רבדים, לא רמה אחת

הדיון עצמו התפתח דרך הבחנה בין שלוש רמות שונות של "עיצוב", עם מסקנה שונה לכל אחת:

| רמה | דוגמה | החלטה |
|---|---|---|
| **תוכן** | כותרת, תיאור, סיפור, CTA, בחירת/סידור תמונות | ✅ כן — ה-AI מצטיין כאן, זה כבר קיים |
| **המלצות עיצוב מובנות** | `emotion: "hope"`, `focus: "children"` (לא צבע/Template ספציפי) | ✅ כן, כ-Metadata — לא UI חדש, לא Mapping עדיין |
| **עיצוב אוטומטי מלא** | `#E63946`, `Border-radius: 14px`, `Font: Poppins` | ❌ לא — זו החלטה של Design System, לא של מודל שפה |

**הנימוק לרמה השלישית**, כפי שנוסח בדיון: "אם מחר תשנו את כל מערכת ה-Themes, לא תרצה לשנות Prompts. לעומת זאת `Tone: Hope` יישאר תמיד נכון — וה-Frontend יחליט איך למפות את זה."

## מה נבנה (מאומת, 2026-07-23)

כל השלושה הבאים נוספו ל**אותה קריאת LLM אחת** שכבר בונה את ה-Brief (Sprint 2) — לא קריאה נוספת, לא עלות/latency נוספים:

### 1. Gallery Curation
כשמצורפות תמונות לקמפיין (עכשיו נשלחות ל-Vision, באותו אופן שה-Facts extractor כבר עושה), ה-AI בוחר בפועל:
- איזו תמונה הכי מתאימה כ-Hero.
- באיזה סדר להציג את השאר בגלריה.
- אילו תמונות עדיף להשמיט לגמרי (מטושטשת, כפולה, לא רלוונטית) — **עם reason**.

מאומת: כששתי תמונות זהות הועלו, ה-AI זיהה נכון שהן כפולות והשמיט אחת מהן — משהו שדורש שיפוט חזותי אמיתי, לא רק קריאת טקסט. התמונות שסומנו כ"מוסתרות" לא מועלות בכלל (לא רק לא מוצגות) — חוסך עלות אחסון.

**אבטחה**: כל קוד תמונה שה-AI מחזיר (`hero`, `gallery[]`, `hidden[]`) מאומת מחדש מול הרשימה האמיתית שנשלחה בפועל (`sanitizeGalleryCuration` ב-`brief.builder.js`) — אותו משמעת גבולות כמו `organizationNumber`/`heroVideoUrl`: אף פעם לא לסמוך על הפלט הגולמי של המודל.

### 2. Design Intent
```json
{ "emotion": "תקווה", "urgency": "medium", "focus": "ילדים חולי סרטן", "energy": "energetic" }
```
לא צבעים, לא Template. Metadata לשכבת Mapping עתידית שעדיין לא קיימת. לא נוסף שום UI חדש להצגה שלו כרגע.

### 3. Content Strategy
```json
{ "primaryAudience": "קהילה מקומית", "storyPattern": "problem-solution", "ctaApproach": "collective-impact" }
```
לא מוצג למשתמש בכלל. Metadata פנימי לצרכנים עתידיים (CampaignAdvisor שמשווה כוונה מול תוצאה בפועל, אנליטיקס, A/B testing).

## מה נדחה במפורש, ולמה

הדיון עצמו זיהה וסינן כמה רעיונות נוספים — לא נבנו, עם נימוק ברור לכל אחד:

- **`templateSuggestion` / `paletteSuggestion`** — **הלקח המנחה**: זו בדיוק אותה טעות שכבר קרתה עם `categoryGuess` לפני שחובר לרשימת 31 הקטגוריות האמיתיות של המערכת (`entity-categories.ts`) — בלי קטלוג Templates/Palettes אמיתי לבחור ממנו, ה-AI היה "ממציא" שמות שלא מתחברים לכלום. נדחה עד שיהיה קטלוג כזה.
- **לולאת Self-Review** (המודל מדרג את עצמו 1-10 על חיבור רגשי/בהירות/אמינות, ומשכתב אם ציון נמוך) — מכפילה עלות וזמן תגובה בכל יצירה, ואין עדיין שום ראיה שציון-עצמי של LLM הוא מדד איכות אמין. נדחה עד שיש דרך למדוד שזה באמת משפר תוצאות.
- **`pageFlow`** (המודל קובע סדר בלוקים — Hero/Impact Numbers/Story/Video/CTA/FAQ) — בר-ביצוע עקרונית, אבל דורש קודם הבנה אילו סוגי בלוקים תמיד קיימים מול אופציונליים בסטודיו. לא נבנה.

## עקרון העל

> ה-AI מתאר את **כוונת** העיצוב (ומקבל החלטות תוכן מלאות), והמערכת — לא המודל — מתרגמת כוונה להחלטת עיצוב בפועל.

זו אותה משמעת בדיוק שמנחה את כל ה-ADR הרחב יותר (`AI_CAMPAIGN_CREATION_VISION.md`): AI מציע, המערכת/המשתמש מחליטים; שום דבר לא "בורח" מחוץ לאוצר האפשרויות הסגור שהמערכת כבר תומכת בו.

## נקודת המשך טבעית

ברגע שיהיה קטלוג Templates/Palettes אמיתי וסגור (מקביל ל-`entity-categories.ts` הקיים), `designIntent` כבר מוכן להיות הקלט לשכבת Mapping כזו — בלי לגעת שוב בפרומפט של ה-Brief.

## Brief כ-Campaign Specification — שינוי קונספטואלי, לא Rename

דיון המשך זיהה נקודה חשובה: `Brief` הוא שם היסטורי שהפך לא-מדויק. כשהוא נולד (Sprint 2), הוא היה באמת בריף טקסטואלי פשוט. היום הוא כבר מכיל תוכן, Hero, Gallery Curation, Tone, Category, החלטות מדיה, תוצאות חיפוש אינטרנט, ופערי מידע (`clarifyingQuestions`) — הרבה מעבר ל"בריף".

**ההחלטה: לא לבצע Rename בקוד.** `Brief` מככב כשם קובץ (`brief.builder.js`), נתיבי endpoint (`/map-to-draft`, `/refine-brief`), טיפוס בפרונטאנד, סיגנל, מחלקות CSS, ולאורך כל ה-ADR הקיים — rename מלא הוא refactor רחב-היקף עם סיכון אמיתי, לתועלת שהיא כמעט כולה רעיונית. השינוי הנכון הוא **קונספטואלי, לא נומנקלטורה**: לתעד את איך חושבים על האובייקט, לא לשנות את שמו.

**איך לחשוב על זה מעכשיו**: למרות השם ההיסטורי, מבחינה ארכיטקטונית `Brief` מהווה את ה-**Campaign Specification v1** של המערכת. עם התפתחות יכולות ה-AI (Media Decisions, Design Intent, Gallery Curation, Content Strategy — כולן כבר קרו) הוא צפוי להתרחב עוד באופן אורגני — אך **לא** יעבור הפשטה או פיצול למנגנון כללי (Directors נפרדים, Framework, Interfaces) לפני שיהיה לכך צורך עסקי אמיתי ומוכח. זו אותה משמעת בדיוק שכבר מוכיחה את עצמה שוב ושוב בפרויקט הזה:

> **No generic abstractions before a second real consumer.**

**עיקרון ארכיטקטוני**: The Brief is an implementation detail today and an architectural boundary tomorrow. היום זה אובייקט שמעביר מידע בין ה-AI ל-`draft.builder.js`. בעתיד הוא עשוי להתפתח ל-Specification עשיר יותר — אך ההתרחבות תמיד תהיה אינקרמנטלית ומונעת-צורך, לא Framework כללי שנבנה מראש.

הארכיטקטורה האמיתית לא נקבעת לפי שם קובץ — היא נקבעת לפי הגבול בין האחריות של הרכיבים (AI מייצר / `draft.builder.js` מממש). הגבול הזה כבר קיים ועובד. השם `Brief` הוא חוב היסטורי קטן וקוסמטי, לא בעיה ארכיטקטונית.
