# Analytics Vision — "המונים"

**סטטוס:** v1.0
**תאריך:** 2026-07-13

## 0. Mission

מטרת מערכת האנליטיקה אינה להציג נתונים — היא לעזור למנהל עמותה לקבל החלטות טובות יותר. כל דוח, כרטיס או תובנה שלא מוביל להחלטה אפשרית, לא ממלא את התפקיד שלו, גם אם הוא נראה טוב.

## 1. למה המסמך הזה קיים

הדוחות באפליקציה גדלו בצורה אורגנית: קודם דשבורד, אחר כך 4 טאבי דוחות (ביצועי קמפיינים, שיווק, מגמות, תקלות), ובקרוב עוד — דוח תורמים, AI Insights, משפך המרה. בלי תפיסה אחת, כל תוסף חדש הופך לעוד מסך נפרד שמציג עוד חתך של אותם נתונים, בלי מקום ברור למי שייך מה.

המסמך הזה לא מגדיר קוד או Query — הוא מגדיר **לאיזו שכבה שייך כל דוח קיים ועתידי**, כדי שכל פיצ'ר חדש ידע איפה הוא נמצא לפני שהוא נכתב.

## 2. ארבע השכבות

| שכבה | השאלה שהיא עונה עליה | דוגמה |
|---|---|---|
| **1. Overview** | מה מצב העמותה עכשיו? | גויס החודש, תרומות, קמפיינים פעילים, התראות |
| **2. Analysis** | למה זה קרה? | ביצועי קמפיינים, שיווק/ערוצים, מגמות, תקלות, תורמים |
| **3. Advisor** | מה כדאי לעשות? | תובנות AI, המלצות פעולה |
| **4. Investigation** | אני רוצה להבין לעומק | טבלאות מלאות, פילטרים, CSV, drill-down |

כל דוח קיים או עתידי צריך להשתייך לשכבה אחת ברורה. אם קשה לשייך דוח לשכבה — זה סימן שהוא בעצם שניים.

**זרימת הניווט**: המשתמש מתחיל תמיד ב-Overview, ורק אם צריך מעמיק ל-Analysis, ומשם ל-Investigation. ה-Advisor (שכבה 3) לא שלב בדרך אלא פרשנות שזמינה מכל נקודה בזרימה — הוא מצטרף לשכבה שבה המשתמש כרגע, לא מחליף אותה.

### 2.1 מיפוי מצב קיים לשכבות (לצורך כיול, לא שינוי מיידי)

- **Overview** — עמוד `/dashboard` הקיים (KPI cards, התראות, גרף 30 יום). זו כבר השכבה הזו בפועל, גם אם היא לא ממוקמת היום כ"כניסה" לדוחות.
- **Analysis** — 4 טאבי הדוחות הקיימים (`campaign-performance-report`, `trends-report`, `marketing-report`, `failures-report`). דוח תורמים עתידי משתייך גם הוא לכאן.
- **Advisor** — `CampaignAdvisorAgent` הקיים. זו הפעם הראשונה שיש נציג אמיתי לשכבה הזו במערכת.
- **Investigation** — היום מעורבב בתוך שכבת ה-Analysis (טבלאות + CSV + פילטרים בתוך אותם טאבים). זו לא בהכרח בעיה בשלב הזה — אבל כשהיא תיפרד (למשל טבלת כל התרומות עם drill-down מלא), היא שייכת לכאן ולא ל-Analysis.

**המסקנה המעשית**: אין צורך לבנות "Executive Dashboard" חדש — יש כבר Overview. הצעד הנכון הוא לחבר את שכבת ה-Overview (`/dashboard`) לשכבת ה-Analysis (`/reports`) לחוויה רציפה אחת, לא שני עולמות נפרדים.

## 3. Design Principles

- כל KPI צריך להיות ניתן ללחיצה — drill-down לפירוט שמאחוריו.
- אין כפילות בין דוחות — כל נתון מופיע במקום אחד בלבד.
- כל דוח חייב לענות על שאלה עסקית ברורה. אם אי אפשר לנסח את השאלה במשפט אחד — הדוח לא בשל.
- AI לעולם לא ימציא עובדות — הוא רק מסביר ומתעדף עובדות שכבר חושבו.
- כל נתון מוצג צריך להיות Actionable — אם המשתמש לא יכול לקבל החלטה בעקבותיו, כנראה שהוא לא צריך להיות מוצג.

## 4. ארכיטקטורת ה-Advisor (שכבה 3)

תובנה חשובה: ה-Advisor הוא לא Agent במובן של מעגל חשיבה רב-שלבי. הוא פייפליין דטרמיניסטי חד-כיווני, בדיוק כפי ש-`CampaignAdvisorAgent` כבר בנוי — עם הפרדה בין **עובדות** ל**תובנות מחושבות**:

```
Tools              ← שליפת נתונים גולמיים
   ↓
Analysis Engine    ← חישוב עובדות אובייקטיביות, ספציפיות לדומיין
   ↓
Facts              ← עובדות גולמיות (למשל: raised_this_month, pct_change)
   ↓
Derived Insights   ← תבניות/מסקנות שנגזרות מהעובדות (למשל: "הקצב ירד ב-30%")
   ↓
Prompt Builder     ← ניסוח ה-Derived Insights לפרומפט
   ↓
LLM                ← פרשנות, תעדוף וניסוח — לא המצאת עובדות או תובנות חדשות
```

השם **Derived Insights** (ולא סתם Insights) מדגיש שמדובר במסקנות מחושבות דטרמיניסטית מתוך Facts — לא במסקנות של ה-LLM. ההפרדה חשובה כי לא כל תובנה דורשת LLM: "הקמפיין ירד ב-30%" הוא חישוב דטרמיניסטי לחלוטין, בלי בינה מלאכותית. ה-LLM נכנס לתמונה רק כשצריך לנסח, לתעדף בין כמה תובנות, או להסביר אותן בשפה טבעית. ההפרדה משאירה פתח לעתיד להריץ חלק מה-Derived Insights בלי קריאת LLM בכלל.

**החכמה נמצאת במנועי ה-Facts וה-Derived Insights, לא ב-LLM.**

המשמעות: `DashboardAdvisor`, `DonorAdvisor`, `OrganizationAdvisor` (עתידיים) חולקים את אותה תשתית טכנית — `llm.service.js`, `trace.util.js`, מבנה ה-prompt — שכבר הוצאה השבוע לרמה המשותפת. אבל **כל Advisor חדש עדיין דורש Analysis Engine משלו**, ספציפי לדומיין (למשל: ל-Donor Advisor — מי חזר, מי נשר, כמה זמן עבר מהתרומה האחרונה). זה לא "עוד Advisor בחינם" — זה תשתית משותפת + עבודת דומיין אמיתית לכל אחד.

## 5. Roadmap

| שלב | מה | שכבה | למה לפני הבא |
|---|---|---|---|
| **Phase 1** | Global Date Range — פילטר זמן גלובלי לכל הדוחות | תשתית חוצה-שכבות | כל שאר הדוחות מסתמכים עליו |
| **Phase 2** | איחוד Dashboard + Reports לחוויה אחת | Overview → Analysis | נותן חוויית משתמש אחידה לפני שמוסיפים עוד תוכן |
| **Phase 3** | Donor Analytics — דוח תורמים (חדשים/חוזרים/נושרים) | Analysis | משתמש בנתונים שכבר קיימים ב-DB |
| **Phase 4** | AI Advisor — הרחבת התבנית הקיימת מעבר ל-Campaign | Advisor | מוסיף שכבת קבלת החלטות מעל דוחות שכבר קיימים |
| **Phase 5** | Conversion Tracking — תשתית איסוף (Page View → CTA → תרומה) | תשתית (לא שכבת UI) | אוסף את הנתונים לפני שמנסים להציג אותם |
| **Phase 6** | Conversion Funnel — הדוח עצמו, רק אחרי ש-Phase 5 קיים | Analysis | צורך נתונים שנאספו ב-Phase 5, לא לפני |

**עיקרון מנחה**: משפך ההמרה (Phase 6) לא מופיע לפני שיש תשתית Tracking (Phase 5). זה מונע בניית דוח על נתונים שלא נאספים.

## 6. Success Criteria

איך נדע שמערכת האנליטיקה טובה:

- מנהל עמותה יכול להבין את מצב הארגון תוך פחות מדקה, בלי לפתוח יותר משני מסכים.
- כל KPI מוביל לפעולה אפשרית — לא רק למספר.
- ה-Advisor מספק המלצות שמבוססות על Facts בלבד, לעולם לא על ניחוש.
- הוספת דוח חדש לא יוצרת כפילות עם דוח קיים (ר' Design Principles).

## 7. מה זה לא

- זה לא מסמך ארכיטקטורת קוד (Functional Spec) — זה נכתב בנפרד לכל Feature כשמגיע תורו, כמו שנעשה עם `CAMPAIGN_ADVISOR_AGENT_FUNCTIONAL_SPEC.md`.
- זה לא מחייב לבנות הכל — זה מגדיר איפה כל דבר *שייך*, אם וכשהוא ייבנה.
- זה מסמך חי — כשמתווסף דוח חדש, קודם בודקים לאיזו שכבה הוא שייך, ורק אז בונים אותו.

---

**במקרה של ספק, עדיף להרחיב שכבה קיימת מאשר ליצור שכבה או מסך חדשים.**
